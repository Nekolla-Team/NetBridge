[workspace]
members = [
    "crates/net-bridge-core",
    "crates/net-bridge-native",
    "xtask",
]
resolver = "2"

[profile.release]
opt-level = 3
lto = true
strip = "symbols"

[workspace.dependencies]
bytes = "1"
